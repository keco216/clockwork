<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Kein Auto-Backup: Ohne Tresor gibt es nichts zu sichern, und der
         verschluesselte Tresor-Umschlag soll das Geraet nicht Richtung
         Google-Backup verlassen — die Web-App laedt auch nichts irgendwohin. -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Berechtigungen.

         INTERNET steht hier bewusst NICHT: Der WebView-Inhalt kommt ueber
         Capacitors lokalen Abfangpfad aus dem APK, nicht ueber ein Socket.
         Ohne die Berechtigung kann der Prozess auf Betriebssystem-Ebene keine
         Verbindung oeffnen — dieselbe Zusage wie connect-src 'none' in der
         Einzeldatei, nur eine Schicht tiefer und im Manifest nachlesbar.

         CAMERA ist fuer den QR-Sucher (getUserMedia): Capacitors
         WebChromeClient uebersetzt die WebView-Anfrage in den
         Android-Berechtigungsdialog, sobald sie hier deklariert ist.
         required="false", weil QR aus Bild auch ohne Kamera funktioniert —
         ein Geraet ohne Kamera darf die App installieren. -->

    <uses-permission android:name="android.permission.CAMERA" />

    <!-- BEIDE Feature-Namen ausdruecklich freiwillig: Die CAMERA-Berechtigung
         allein wuerde android.hardware.camera stillschweigend zur Pflicht
         machen (implizites uses-feature), und camera.any deckt das nicht ab —
         nachgemessen mit aapt2 dump badging. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />
</manifest>
