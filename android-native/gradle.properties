org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
# Kein Jetifier: Es gibt in diesem Projekt keine Support-Library-Abhaengigkeit,
# die uebersetzt werden muesste. Eingeschaltet kostet er nur Bauzeit.
android.enableJetifier=false

kotlin.code.style=official

# Der Metadatenblock im APK/AAB ist eine verschluesselte, nur fuer Google
# lesbare Abhaengigkeitsliste im Signaturblock. F-Droid lehnt ihn ab; die
# 1.x-Fassung schaltet ihn deshalb aus, und diese Fassung tut dasselbe (der
# eigentliche Schalter steht in app/build.gradle.kts — hier steht nur, warum).
